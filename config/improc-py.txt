# Science Python for /opt/astroai/venv/improc (uv). Unpinned — resolve at image build.
# Not installed into the CADC venv.
# MaxiMask/TF share this venv (tensorflow>=2.11).
# torch + torchfits (CUDA 12.9 index) installed in the Dockerfile.

# Core image processing
astropy
ccdproc
photutils
galsim
piff
fitsio
sep
reproject
drizzle
regions
statmorph

# Cosmic rays / cleaning / weights
astroscrappy
lacosmic
astroimtools
astroalign
tweakwcs
weightmask

# Modern difference-image analysis (DIA)
sfft
zogyp

# Deblending / forward modeling / astrometry
# scarlet + tractor: built from git in the Dockerfile (no usable PyPI wheels).
scarlet2
twirl

# Galaxy profile / morphology fitting
petrofit
galight
lenstronomy

# Contaminant masks (pulls TensorFlow)
MaxiMask

# General-purpose imaging + archive access
scikit-image
opencv-python-headless
astroquery
montage-wrapper

# Spherical / HEALPix analysis
healpy
healsparse
astropy-healpix
mocpy
hpgeom
